
- 구조체를 이용한 3차원 좌표표시
struct ThreeDime {
    double x;
    double y;
    double z;
};

- 3차원상에서 한 점과 원점의 거리와 두 점간의 거리 구하기
    - A1(x1, y1, z1) 점과 원점의 거리 
    D = ROOT( x1^2 + y1^2 + z1^2)

    - A1(x1, y1, z1)점과 A2(x2, y2, z2) 점의 거리
    D = ROOT(( x1 - x2)^2 + ( y1 - y2 )^2 + (z1 - z2)^2)

- ROOT와 같은 기능을 수행하는 함수는 sqrt()인데, 이 함수는 헤더파일인 math.h에 정의되어 있다.